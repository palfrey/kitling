imager:
  build: imager/
comparer:
  build: comparer/
  links:
    - postgres
  volumes:
    - ./comparer/:/comparer/
  environment:
    - DATABASE_URL=postgres://postgres:mysecretpassword@postgres/postgres
frontend:
  build: frontend/
  ports:
    - "8000:8000"
  links:
    - postgres
  environment:
    - DATABASE_URL=postgres://postgres:mysecretpassword@postgres/postgres
postgres:
  image: postgres
  ports:
    - "5432:5432"
  environment:
    - POSTGRES_PASSWORD=mysecretpassword
